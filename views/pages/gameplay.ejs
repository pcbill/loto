<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h3>
      Gameplay
      </h3>

<!--
      <form action="/registerSubmit" method="POST">
      uid <input name="uid" type="text" />
        <button type="submit" value="submit">submit</button>
      </form>
      <br/>
-->
      <% if (results) { %>
        <br/>
        There are <%= results.length %> gameplaies
        <table class="table table-bordered table-hover">
          <tr>
            <th>operation</th>
            <th>#</th>
            <th>gid</th>
            <th>award list</th>
            <th>count</th>
          </tr>
        <% results.forEach( (it) => { %>
          <tr>
            <td><a class="btn btn-primary" href='/deleteGame/<%= it.id %>'>del</a></td>
            <td><%= it.id %></td>
            <td><%= it.gid %></td>
            <td><%= it.award_list %></td>
            <td><%= it.participant_count%></td>
          </tr>
        <% }); %>
        </table>
      <% } %>
 
    </div>
  </div>
</div>


</body>
</html>
