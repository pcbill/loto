<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h3>歡迎!!</h3>

      <% if (results) { %>
        共有 <%= results.length %> 次抽獎
        <table class="table table-bordered table-hover">
          <tr>
            <th>獎項</th>
            <th>獎品列表</th>
            <th>數量</th>
          </tr>
        <% results.forEach( (it) => { %>
          <tr>
            <td><%= it.gid %></td>
            <td><%= it.award_list %></td>
            <td><%= it.participant_count%></td>
          </tr>
        <% }); %>
        </table>
      <% } %>

    </div>
  </div>
</div>


</body>
</html>
