
<!DOCTYPE html>
<html>
<head>
  <%- include('../partials/header.ejs') %>

</head>

<body>
  <%- include('../partials/nav.ejs') %>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h3>
        得獎者列表
      </h3>
    
      <% if (isDrawing) { %>
      <div class="alert alert-warning" style="font-size: 24px; text-align: center; padding: 50px;">
        <h2>🎰 開獎進行中...</h2>
        <p>請稍候，獎項正在揭曉中！</p>
        <p>請勿重新整理此頁面</p>
        <br/>
        <button class="btn btn-primary" onclick="location.reload()">重新整理查看結果</button>
      </div>
      <% } else if (results && results.length > 0) { %>
      <table class="table table-bordered table-hover">
          <tr>
            <th>姓名</th>
            <th>桌次</th>  
            <th>中獎獎品</th>
            <th>中獎時間</th>
            <th>領獎時間</th>
          </tr>
        <% results.forEach( (it) => { %>
          <tr>
            <td><%= it.name %></td>
            <td><%= it.table_num %></td> 
            <td><%= it.awardList %></td>
            <td><%= it.award_time %></td>
            <td><%= it.getgift_time %></td>
          </tr>
        <% }); %>
      </table>
      <% } else { %>
      <p>尚無中獎者</p>
      <% } %>

    </div>
  </div>
</div>
