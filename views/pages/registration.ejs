<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h3>
      Registration
      </h3>

      <form action="/registerSubmit" method="POST">
      uid <input id="uid" name="uid" type="text" forcus />
        <button type="submit" value="submit">submit</button>
      </form>
      <br/>
      <% if (results) { %>
        <br/>
        There are <%= results.length %> people registered
        <table class="table table-bordered table-hover">
          <tr>
            <th>#</th>  
            <th>uid</th>  
            <th>name</th>  
            <th>registration time</th>  
          </tr>
        <% results.forEach( (it) => { %>
          <tr>
            <td><%= it.id %></td>
            <td><%= it.uid %></td> 
            <td><%= it.name %></td> 
            <td><%= it.registration_time %></td>
          </tr>
        <% }); %>
        </table>
      <% } %>
    </div>
  </div>
</div>


</body>

<script> document.getElementById("uid").focus(); </script>
</html>
