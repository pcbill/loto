<!DOCTYPE HTML>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <%- include('../partials/header.ejs') %>
<title>屏東科技大學 尾牙抽獎</title>

<link href='https://fonts.googleapis.com/css?family=Gravitas+One&text=1234567' rel='stylesheet' type='text/css'>
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;700;900&display=swap" rel="stylesheet">

<style type="text/css">

* {
    font-family: 'Noto Sans TC', sans-serif;
}

body {
    margin: 0;
    padding: 0;
    min-height: 100vh;
    background: linear-gradient(135deg, #1a0a0a 0%, #4a1515 30%, #8B0000 60%, #4a1515 100%);
    background-attachment: fixed;
    position: relative;
    overflow-x: hidden;
}

/* 背景裝飾 - 金色光暈 */
body::before {
    content: '';
    position: fixed;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(ellipse at center, rgba(255, 215, 0, 0.1) 0%, transparent 50%);
    animation: shimmer 8s ease-in-out infinite;
    pointer-events: none;
}

@keyframes shimmer {
    0%, 100% { transform: translate(0, 0) rotate(0deg); opacity: 0.3; }
    50% { transform: translate(5%, 5%) rotate(5deg); opacity: 0.6; }
}

/* 飄落的金幣/紅包動畫 */
.falling-item {
    position: fixed;
    font-size: 30px;
    animation: fall linear infinite;
    pointer-events: none;
    z-index: 1;
}

@keyframes fall {
    0% { transform: translateY(-100px) rotate(0deg); opacity: 1; }
    100% { transform: translateY(100vh) rotate(720deg); opacity: 0.3; }
}

ul {
    padding: 0;
    margin: 0;
    list-style: none;
}

/* 主容器 */
.lottery-container {
    position: relative;
    z-index: 10;
    padding: 20px;
}

/* 頂部導航 */
.top-nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 30px;
    background: rgba(0, 0, 0, 0.3);
    border-bottom: 2px solid #FFD700;
}

.back-btn {
    display: inline-flex;
    align-items: center;
    padding: 10px 20px;
    background: linear-gradient(135deg, #FFD700, #FFA500);
    color: #8B0000;
    text-decoration: none;
    font-weight: bold;
    border-radius: 25px;
    transition: all 0.3s;
    box-shadow: 0 4px 15px rgba(255, 215, 0, 0.3);
}

.back-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(255, 215, 0, 0.5);
    text-decoration: none;
    color: #8B0000;
}

.back-btn::before {
    content: '←';
    margin-right: 8px;
    font-size: 18px;
}

/* 學校標誌區 */
.school-header {
    text-align: center;
    padding: 20px 0;
}

.school-name {
    font-size: 28px;
    font-weight: 900;
    color: #FFD700;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5), 0 0 30px rgba(255, 215, 0, 0.3);
    letter-spacing: 8px;
    margin-bottom: 10px;
}

.event-year {
    font-size: 18px;
    color: #FFA500;
    letter-spacing: 4px;
}

/* 獎項標題 + 剩餘次數 + 開始按鈕 一行排列 */
.prize-control-row {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 40px;
    margin: 30px auto;
    padding: 20px;
    flex-wrap: wrap;
}

.prize-title {
    display: inline-flex;
    align-items: center;
    padding: 15px 40px;
    background: linear-gradient(135deg, #FFD700 0%, #FFA500 50%, #FFD700 100%);
    border-radius: 15px;
    box-shadow: 0 10px 40px rgba(255, 215, 0, 0.4), inset 0 2px 0 rgba(255, 255, 255, 0.3);
}

.prize-title::before,
.prize-title::after {
    content: '🏆';
    font-size: 30px;
    margin: 0 10px;
}

.prize-title h2 {
    margin: 0;
    font-size: 28px;
    font-weight: 900;
    color: #8B0000;
    text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.5);
}

/* 剩餘次數 - 行內樣式 */
.remaining-count {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 12px 25px;
    background: rgba(0, 0, 0, 0.5);
    border-radius: 50px;
    border: 2px solid #FFD700;
}

.remaining-count img {
    height: 40px;
}

.remaining-count .count-number {
    font-size: 36px;
    font-weight: 900;
    color: #FFD700;
    text-shadow: 0 0 20px rgba(255, 215, 0, 0.5);
    min-width: 40px;
    text-align: center;
}

.remaining-count .count-label {
    font-size: 16px;
    color: #FFA500;
}

/* 老虎機 + 按鈕容器 */
.slot-machine-wrapper {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 30px;
    margin: 40px 0;
}

/* 抽獎按鈕區域 */
.draw-button-section {
    display: flex;
    align-items: center;
    justify-content: center;
}

/* 抽獎按鈕 */
.draw-button {
    position: relative;
    padding: 25px 60px;
    font-size: 32px;
    font-weight: 900;
    color: #8B0000;
    background: linear-gradient(135deg, #FFD700 0%, #FFA500 50%, #FFD700 100%);
    border: none;
    border-radius: 60px;
    cursor: pointer;
    box-shadow: 0 8px 30px rgba(255, 215, 0, 0.5),
                0 4px 15px rgba(0, 0, 0, 0.3),
                inset 0 2px 0 rgba(255, 255, 255, 0.4);
    transition: all 0.3s ease;
    text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.5);
    letter-spacing: 4px;
}

.draw-button::before {
    content: '🎊';
    margin-right: 15px;
    font-size: 36px;
}

.draw-button::after {
    content: '🎊';
    margin-left: 15px;
    font-size: 36px;
}

.draw-button:hover {
    transform: translateY(-5px) scale(1.05);
    box-shadow: 0 15px 40px rgba(255, 215, 0, 0.6),
                0 8px 20px rgba(0, 0, 0, 0.3),
                inset 0 2px 0 rgba(255, 255, 255, 0.5);
    background: linear-gradient(135deg, #FFE44D 0%, #FFB732 50%, #FFE44D 100%);
}

.draw-button:active {
    transform: translateY(2px) scale(0.98);
    box-shadow: 0 4px 15px rgba(255, 215, 0, 0.4),
                0 2px 8px rgba(0, 0, 0, 0.2);
}

.draw-button.clicked {
    animation: buttonPulse 0.5s ease;
}

@keyframes buttonPulse {
    0% { transform: scale(1); }
    50% { transform: scale(0.95); }
    100% { transform: scale(1); }
}

/* 禁用狀態 */
.draw-button.disabled {
    cursor: not-allowed;
    background: linear-gradient(135deg, #888 0%, #666 50%, #888 100%);
    color: #444;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.draw-button.disabled:hover {
    transform: none;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.draw-button.disabled::before,
.draw-button.disabled::after {
    opacity: 0.5;
}

/* 老虎機外殼 */
.slot-machine {
    background: linear-gradient(180deg, #3a1515 0%, #2a0a0a 50%, #1a0505 100%);
    border: 10px solid #FFD700;
    border-radius: 25px;
    padding: 40px 20px;
    box-shadow: 0 0 60px rgba(255, 215, 0, 0.4), 
                inset 0 0 60px rgba(0, 0, 0, 0.6),
                0 20px 40px rgba(0, 0, 0, 0.5);
    position: relative;
    width: 90%;
    max-width: 900px;
    margin: 0 auto;
}

.slot-machine::before {
    content: '🎰 幸運抽獎機 🎰';
    position: absolute;
    top: -28px;
    left: 50%;
    transform: translateX(-50%);
    background: linear-gradient(135deg, #FFD700, #FFA500);
    padding: 10px 40px;
    border-radius: 25px;
    font-weight: bold;
    color: #8B0000;
    font-size: 18px;
    white-space: nowrap;
    box-shadow: 0 4px 15px rgba(255, 215, 0, 0.5);
}

.jSlots-wrapper {
    overflow: hidden;
    height: 20px;
    display: inline-block;
    border: 1px solid #999;
}

.slot {
    float: left;
}

/* 老虎機樣式 */
.fancy .jSlots-wrapper {
    overflow: hidden;
    line-height: 200px;
    height: 200px;
    display: inline-block;
    border: none;
    border-radius: 15px;
    box-shadow: inset 0 0 30px rgba(0, 0, 0, 0.8);
    width: 100%;
    max-width: 90vw;
}

.fancy .slot li {
    width: 90vw;
    max-width: 1000px;
    line-height: 200px;
    height: 200px;
    text-align: center;
    font-size: clamp(28px, 6vw, 80px);
    font-weight: 900;
    color: #fff;
    text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.8), 0 0 20px rgba(255, 215, 0, 0.5);
    border-left: none;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    padding: 0 20px;
    box-sizing: border-box;
}

.fancy .slot:first-child li {
    border-left: none;
}

/* 漸變色背景 - 喜慶紅金配色 */
.fancy .slot li {
    background: linear-gradient(135deg, #DC143C 0%, #8B0000 100%);
}
.fancy .slot li:nth-child(7) {
    background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
    color: #8B0000;
}
.fancy .slot li:nth-child(6) {
    background: linear-gradient(135deg, #FF4500 0%, #DC143C 100%);
}
.fancy .slot li:nth-child(5) {
    background: linear-gradient(135deg, #FF6347 0%, #FF4500 100%);
}
.fancy .slot li:nth-child(4) {
    background: linear-gradient(135deg, #FFD700 0%, #FF8C00 100%);
    color: #8B0000;
}
.fancy .slot li:nth-child(3) {
    background: linear-gradient(135deg, #DC143C 0%, #B22222 100%);
}
.fancy .slot li:nth-child(2) {
    background: linear-gradient(135deg, #8B0000 0%, #4a0000 100%);
}
.fancy .slot li:nth-child(1) {
    background: linear-gradient(135deg, #FF0000 0%, #CC0000 100%);
}

.fancy .slot li span {
    display: block;
}

/* 得獎者列表 */
.winners-section {
    max-width: 800px;
    margin: 40px auto;
    padding: 0 20px;
}

.winners-title {
    text-align: center;
    color: #FFD700;
    font-size: 24px;
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
}

.winners-title::before,
.winners-title::after {
    content: '';
    flex: 1;
    height: 2px;
    background: linear-gradient(90deg, transparent, #FFD700, transparent);
}

.winners-table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0 8px;
}

.winners-table th {
    background: linear-gradient(135deg, #FFD700, #FFA500);
    color: #8B0000;
    padding: 15px 20px;
    font-size: 18px;
    font-weight: bold;
}

.winners-table th:first-child {
    border-radius: 10px 0 0 10px;
}

.winners-table th:last-child {
    border-radius: 0 10px 10px 0;
}

.winners-table td {
    background: rgba(255, 255, 255, 0.95);
    padding: 15px 20px;
    font-size: 20px;
    font-weight: 500;
    color: #333;
}

.winners-table tr td:first-child {
    border-radius: 10px 0 0 10px;
}

.winners-table tr td:last-child {
    border-radius: 0 10px 10px 0;
}

.winners-table .btn {
    background: linear-gradient(135deg, #DC143C, #8B0000);
    border: none;
    color: white;
    padding: 8px 20px;
    border-radius: 20px;
    font-weight: bold;
    transition: all 0.3s;
}

.winners-table .btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(220, 20, 60, 0.4);
}

/* 中獎動畫 */
@keyframes winner {
    0%, 50%, 100% {
        transform: rotate(0deg) scale(1);
        color: #fff;
    }
    25% {
        transform: rotate(10deg) scale(1.1);
        color: #FFD700;
    }
    75% {
        transform: rotate(-10deg) scale(1.1);
        color: #FFD700;
    }
}

@keyframes winnerBox {
    0%, 50%, 100% {
        box-shadow: inset 0 0 0px #FFD700;
        background: linear-gradient(135deg, #DC143C, #8B0000);
    }
    25%, 75% {
        box-shadow: inset 0 0 50px #FFD700;
        background: linear-gradient(135deg, #FFD700, #FFA500);
    }
}

.winner li {
    animation: winnerBox 1s infinite linear;
}
.winner li span {
    animation: winner 1s infinite linear;
}

/* 裝飾元素 */
.decoration {
    position: fixed;
    pointer-events: none;
    z-index: 5;
}

.lantern {
    position: fixed;
    font-size: 60px;
    animation: sway 3s ease-in-out infinite;
}

.lantern-left {
    top: 80px;
    left: 30px;
}

.lantern-right {
    top: 80px;
    right: 30px;
    animation-delay: 1.5s;
}

@keyframes sway {
    0%, 100% { transform: rotate(-5deg); }
    50% { transform: rotate(5deg); }
}

/* 隱藏原生按鈕 */
#playFancy {
    display: none;
}

/* 響應式設計 */
@media screen and (max-width: 768px) {
    .slot-machine {
        padding: 30px 15px;
        border-width: 6px;
    }
    
    .slot-machine::before {
        font-size: 14px;
        padding: 8px 20px;
    }
    
    .fancy .jSlots-wrapper {
        height: 150px;
        line-height: 150px;
    }
    
    .fancy .slot li {
        height: 150px;
        line-height: 150px;
        font-size: clamp(20px, 5vw, 50px);
        padding: 0 10px;
    }
    
    .draw-button {
        padding: 18px 40px;
        font-size: 24px;
    }
    
    .draw-button::before,
    .draw-button::after {
        font-size: 28px;
    }
    
    .prize-title h2 {
        font-size: 20px;
    }
    
    .remaining-count .count-number {
        font-size: 28px;
    }
}

@media screen and (max-width: 480px) {
    .slot-machine {
        padding: 25px 10px;
        border-width: 4px;
        border-radius: 15px;
    }
    
    .slot-machine::before {
        font-size: 12px;
        padding: 6px 15px;
        top: -22px;
    }
    
    .fancy .jSlots-wrapper {
        height: 120px;
        line-height: 120px;
    }
    
    .fancy .slot li {
        height: 120px;
        line-height: 120px;
        font-size: clamp(16px, 4.5vw, 36px);
        padding: 0 8px;
    }
    
    .draw-button {
        padding: 15px 30px;
        font-size: 20px;
        letter-spacing: 2px;
    }
    
    .draw-button::before,
    .draw-button::after {
        font-size: 24px;
        margin: 0 8px;
    }
    
    .prize-control-row {
        gap: 15px;
        padding: 10px;
    }
    
    .prize-title {
        padding: 10px 20px;
    }
    
    .prize-title h2 {
        font-size: 16px;
    }
    
    .remaining-count {
        padding: 8px 15px;
    }
    
    .remaining-count .count-number {
        font-size: 24px;
    }
    
    .remaining-count .count-label {
        font-size: 12px;
    }
    
    .lantern {
        font-size: 40px;
    }
}

</style>

</head>
<body>

<!-- 飄落裝飾 -->
<script>
(function() {
    const items = ['🧧', '💰', '🎊', '✨', '🏮', '🎉'];
    for (let i = 0; i < 15; i++) {
        const item = document.createElement('div');
        item.className = 'falling-item';
        item.textContent = items[Math.floor(Math.random() * items.length)];
        item.style.left = Math.random() * 100 + 'vw';
        item.style.animationDuration = (Math.random() * 5 + 5) + 's';
        item.style.animationDelay = (Math.random() * 5) + 's';
        document.body.appendChild(item);
    }
})();
</script>

<!-- 燈籠裝飾 -->
<div class="lantern lantern-left">🏮</div>
<div class="lantern lantern-right">🏮</div>

<div class="lottery-container">
    <!-- 頂部導航 -->
    <div class="top-nav">
        <a href="/gameplay" class="back-btn">返回管理</a>
        <div class="school-name">國立屏東科技大學</div>
        <div class="event-year">尾牙抽獎</div>
    </div>

    <!-- 獎項標題 + 剩餘次數 -->
    <div class="prize-control-row">
        <!-- 獎項標題 -->
        <div class="prize-title">
            <h2><%= gameName %></h2>
        </div>

        <!-- 剩餘次數 -->
        <div class="remaining-count">
            <img src="/img/still.png" alt="" />
            <span class="count-number" id="reminderCount"><%= reminderCount %></span>
            <span class="count-label">次機會</span>
        </div>
    </div>

    <!-- 老虎機 + 拉桿 -->
    <div class="slot-machine-wrapper">
        <div class="slot-machine">
            <div class="fancy">
                <ul class="slot" style="visibility: hidden">
                    <% if (results) { %>
                        <li><span>❓❓❓</span></li>
                        <% results.forEach((it) => { %>
                            <li><span><%= it.split(" ")[0] %> <%= it.split(" ")[1] %></span></li>
                        <% }); %>
                    <% } %>
                </ul>
                <input type="button" id="playFancy" value="Play">
            </div>
        </div>
        
    </div>
    
    <!-- 抽獎按鈕 -->
    <div class="draw-button-section">
        <% if (reminderCount > 0) { %>
        <span id="playNextButton">
            <button class="draw-button" onclick="startDraw('/execute/<%= gid %>/true')">
                開始抽獎
            </button>
        </span>
        <% } else { %>
        <span id="playNextButton">
            <button class="draw-button disabled" disabled>
                已抽完
            </button>
        </span>
        <% } %>
    </div>

    <!-- 得獎者列表 -->
    <% if (winners && winners.length > 0) { %>
    <div class="winners-section">
        <h3 class="winners-title">🎊 得獎名單 🎊</h3>
        <table class="winners-table" id="winnerList">
            <thead>
                <tr>
                    <th>號碼</th>
                    <th>姓名</th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
                <% winners.forEach((it) => { %>
                <tr>
                    <td><%= it.uid %></td>
                    <td><%= it.name %></td>
                    <td></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <% } else { %>
    <div class="winners-section">
        <h3 class="winners-title">🎊 得獎名單 🎊</h3>
        <table class="winners-table" id="winnerList">
            <thead>
                <tr>
                    <th>號碼</th>
                    <th>姓名</th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
    <% } %>

</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7/jquery.min.js"></script>
<script src="js/jquery.easing.1.3.js" type="text/javascript" charset="utf-8"></script>
<script src="js/jquery.jSlots.js" type="text/javascript" charset="utf-8"></script>
<script type="text/javascript" charset="utf-8">
    // 抽獎按鈕動畫函數
    function startDraw(url) {
        var btn = document.querySelector('.draw-button');
        if (btn.classList.contains('disabled')) return;
        
        btn.classList.add('clicked');
        
        // 動畫結束後跳轉
        setTimeout(function() {
            window.location.href = url;
        }, 500);
    }
    var winnerUid = '<%= results && results.length > 0 ? results[0].split(" ")[0] : "" %>';
    var winnerName = '<%= results && results.length > 0 ? results[0].split(" ")[1] : "" %>';
    var gid = '<%= gid %>';
    var playRightNow = '<%= playRightNow %>';
    
    function cancelButtonAction() {
        $.get("/cancelWinnerQuietly/" + gid + "/" + winnerUid, function(data) {});

        $('#cancelRow').hide();
        $('#reminderCount').text('<%= reminderCount %>');

        $('#playNextButton').html('<button class="draw-button" onclick="startDraw(\'/execute/' + gid + '/true\')">' +
            '開始抽獎' +
            '</button>');
    }

    // 老虎機設定
    $('.fancy .slot').jSlots({
        number: 1,
        winnerNumber: 2,
        spinner: '#playFancy',
        easing: 'easeOutSine',
        time: 7000,
        loops: 4,
        onStart: function() {
            $('.slot').removeClass('winner');
        },
        onWin: function(winCount, winners) {
            $('#reminderCount').text('<%= reminderCount - 1 %>');

            <% if (reminderCount - 1 <= 0) { %>
                $('#playNextButton').html('<button class="draw-button disabled" disabled>' +
                    '已抽完' +
                    '</button>');
            <% } %>

            $('#winnerList tbody').prepend('<tr id="cancelRow">\n' +
                '<td>' + winnerUid + '</td>' +
                '<td style="font-weight: bold; color: #DC143C;">' + winnerName + ' 🎉</td>' +
                '<td><button onclick="cancelButtonAction()" class="btn">取消資格</button></td>\n' +
                '</tr>');

            // 動畫結束後才真正寫入中獎結果
            if (playRightNow === 'true') {
                $.post('/completeBigDrawing/' + gid + '/' + winnerUid, function(data) {
                    if (data.success) {
                        console.log('大獎開獎結果已儲存');
                    }
                }).fail(function(err) {
                    console.error('儲存開獎結果失敗', err);
                    alert('儲存開獎結果失敗，請聯繫管理員');
                });
            } else {
                $.get("/updatePlayerGameRelation/" + gid + "/" + winnerUid, function(data) {});
            }
        }
    });

    $('.slot').css("visibility", "visible");

    <% if (playRightNow === 'true') { %>
        setTimeout(function() {
            $('#playFancy').click();
        }, 1000);
    <% } %>
</script>

</body>
</html>
