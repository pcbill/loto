<!DOCTYPE HTML>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <%- include('../partials/header.ejs') %>
<title>上傳資料 - 屏東科技大學 尾牙抽獎</title>

<style type="text/css">
body {
    background: linear-gradient(135deg, #1a0a0a 0%, #4a1515 30%, #8B0000 60%, #4a1515 100%);
    background-attachment: fixed;
    min-height: 100vh;
}

.upload-container {
    max-width: 800px;
    margin: 30px auto;
    padding: 20px;
}

.page-title {
    text-align: center;
    color: #FFD700;
    font-size: 28px;
    margin-bottom: 30px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

.page-title::before,
.page-title::after {
    content: '📁';
    margin: 0 15px;
}

.upload-section {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 15px;
    padding: 30px;
    margin-bottom: 25px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    border: 2px solid #FFD700;
}

.section-title {
    color: #8B0000;
    font-size: 22px;
    font-weight: bold;
    margin-bottom: 20px;
    padding-bottom: 10px;
    border-bottom: 2px solid #FFD700;
    display: flex;
    align-items: center;
    gap: 10px;
}

.section-title .icon {
    font-size: 28px;
}

.upload-form {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.file-input-wrapper {
    position: relative;
    display: flex;
    align-items: center;
    gap: 15px;
}

.file-input {
    flex: 1;
    padding: 12px 15px;
    border: 2px dashed #ccc;
    border-radius: 8px;
    background: #f9f9f9;
    cursor: pointer;
    transition: all 0.3s;
}

.file-input:hover {
    border-color: #FFD700;
    background: #fff;
}

.file-input:focus {
    outline: none;
    border-color: #8B0000;
}

.upload-btn {
    padding: 12px 30px;
    background: linear-gradient(135deg, #FFD700, #FFA500);
    color: #8B0000;
    border: none;
    border-radius: 8px;
    font-size: 16px;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.3s;
    box-shadow: 0 4px 15px rgba(255, 215, 0, 0.3);
}

.upload-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(255, 215, 0, 0.5);
}

.upload-btn:disabled {
    background: #ccc;
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
}

.help-text {
    color: #666;
    font-size: 14px;
    margin-top: 10px;
    padding: 10px;
    background: #f5f5f5;
    border-radius: 5px;
    border-left: 4px solid #FFD700;
}

.help-text strong {
    color: #8B0000;
}

.result-message {
    padding: 15px;
    border-radius: 8px;
    margin-top: 15px;
    display: none;
}

.result-message.success {
    background: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
    display: block;
}

.result-message.error {
    background: #f8d7da;
    color: #721c24;
    border: 1px solid #f5c6cb;
    display: block;
}

.template-download {
    margin-top: 15px;
    padding-top: 15px;
    border-top: 1px solid #eee;
}

.template-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: #8B0000;
    text-decoration: none;
    font-weight: bold;
    padding: 8px 15px;
    background: #f5f5f5;
    border-radius: 5px;
    transition: all 0.3s;
}

.template-link:hover {
    background: #FFD700;
    text-decoration: none;
}

.template-link.disabled-link {
    opacity: 0.5;
    cursor: not-allowed;
    pointer-events: none;
}

.template-link.disabled-link:hover {
    background: #f5f5f5;
}

.back-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: #FFD700;
    text-decoration: none;
    font-size: 16px;
    margin-bottom: 20px;
    transition: all 0.3s;
}

.back-link:hover {
    color: #fff;
    text-decoration: none;
}

/* 上傳進度 */
.upload-progress {
    display: none;
    margin-top: 15px;
}

.upload-progress.show {
    display: block;
}

.progress-bar {
    width: 100%;
    height: 20px;
    background: #e9ecef;
    border-radius: 10px;
    overflow: hidden;
}

.progress-fill {
    height: 100%;
    background: linear-gradient(90deg, #FFD700, #FFA500);
    width: 0%;
    transition: width 0.3s;
}

.progress-text {
    text-align: center;
    margin-top: 5px;
    color: #666;
    font-size: 14px;
}

/* 警告訊息 */
.warning-box {
    background: #fff3cd;
    color: #856404;
    padding: 15px;
    border-radius: 8px;
    margin-bottom: 20px;
    border: 1px solid #ffc107;
}

.warning-box strong {
    display: block;
    margin-bottom: 5px;
}

@media (max-width: 768px) {
    .upload-container {
        padding: 15px;
    }
    
    .file-input-wrapper {
        flex-direction: column;
    }
    
    .upload-btn {
        width: 100%;
    }
    
    .page-title {
        font-size: 22px;
    }
}

/* 長條圖樣式 */
.chart-container {
    padding: 20px 0;
}

.chart-info {
    margin-bottom: 15px;
    padding: 12px 15px;
    background: #f8f9fa;
    border-radius: 8px;
    font-size: 16px;
}

.chart-legend {
    display: flex;
    gap: 25px;
    margin-bottom: 20px;
    padding: 10px 15px;
    background: #fff;
    border-radius: 6px;
    border: 1px solid #e9ecef;
}

.legend-item {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    color: #555;
}

.legend-color {
    width: 20px;
    height: 12px;
    border-radius: 3px;
}

.legend-color.winner {
    background: linear-gradient(90deg, #FFD700, #FFA500);
}

.legend-color.total {
    background: linear-gradient(90deg, #6c9bd1, #4a7fb8);
}

.bar-chart {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.bar-group {
    display: flex;
    align-items: center;
    gap: 10px;
}

.bar-label {
    width: 70px;
    font-size: 14px;
    font-weight: 500;
    color: #333;
    text-align: right;
    flex-shrink: 0;
}

.bar-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 3px;
}

.bar-row-dual {
    display: flex;
    align-items: center;
    gap: 8px;
}

.bar-wrapper {
    flex: 1;
    height: 16px;
    background: #f0e6c8;
    border-radius: 3px;
    overflow: hidden;
}

.bar-wrapper.total-wrapper {
    background: #d6e4f0;
}

.bar {
    height: 100%;
    border-radius: 3px;
    min-width: 2px;
    transition: width 0.3s ease;
}

.bar.winner-bar {
    background: linear-gradient(90deg, #FFD700, #FFA500);
}

.bar.total-bar {
    background: linear-gradient(90deg, #6c9bd1, #4a7fb8);
}

.bar-value {
    width: 35px;
    font-size: 12px;
    font-weight: bold;
    text-align: left;
    flex-shrink: 0;
}

.bar-value.winner-value {
    color: #b8860b;
}

.bar-value.total-value {
    color: #4a7fb8;
}

.bar-ratio {
    width: 55px;
    font-size: 13px;
    font-weight: bold;
    color: #8B0000;
    text-align: center;
    background: #fff3cd;
    padding: 4px 8px;
    border-radius: 4px;
    flex-shrink: 0;
}

@media (max-width: 768px) {
    .bar-label {
        width: 55px;
        font-size: 12px;
    }
    
    .bar-value {
        width: 30px;
        font-size: 11px;
    }
    
    .bar-ratio {
        width: 45px;
        font-size: 11px;
    }
    
    .chart-legend {
        flex-direction: column;
        gap: 8px;
    }
}
</style>

</head>
<body>
<%- include('../partials/nav.ejs') %>

<div class="upload-container">
    <h1 class="page-title">上傳資料</h1>
    
    <!-- 上傳功能開關 -->
    <div class="upload-section" style="margin-bottom: 25px;">
        <h2 class="section-title">
            <span class="icon">🔐</span>
            上傳功能控制
        </h2>
        <div style="display: flex; align-items: center; gap: 20px; flex-wrap: wrap;">
            <div style="font-size: 18px;">
                目前狀態：
                <span id="uploadStatus" style="font-weight: bold; padding: 5px 15px; border-radius: 5px; <%= uploadEnabled ? 'background: #d4edda; color: #155724;' : 'background: #f8d7da; color: #721c24;' %>">
                    <%= uploadEnabled ? '✅ 已啟用' : '🚫 已停用' %>
                </span>
            </div>
            <div style="display: flex; gap: 10px; align-items: center;">
                <input type="password" id="adminPassword" placeholder="輸入 admin 密碼" 
                       style="padding: 10px 15px; border: 2px solid #ccc; border-radius: 8px; font-size: 14px;">
                <button type="button" id="toggleUploadBtn" class="upload-btn" style="padding: 10px 20px;">
                    🔄 切換開關
                </button>
            </div>
        </div>
        <div id="toggleResult" style="margin-top: 10px;"></div>
    </div>
    
    <div class="warning-box">
        <strong>⚠️ 注意事項</strong>
        上傳新資料將會覆蓋系統中的現有資料，請確認後再進行上傳。建議先下載範本確認格式正確。
    </div>

    <!-- 上傳獎項 -->
    <div class="upload-section">
        <h2 class="section-title">
            <span class="icon">🎁</span>
            上傳獎項資料
        </h2>
        
        <form class="upload-form" id="gameUploadForm" enctype="multipart/form-data">
            <div class="file-input-wrapper">
                <input type="file" name="gameFile" id="gameFile" class="file-input" 
                       accept=".xlsx,.xls,.csv" required <%= uploadEnabled ? '' : 'disabled' %>>
                <button type="submit" class="upload-btn" id="gameUploadBtn" <%= uploadEnabled ? '' : 'disabled' %>>
                    📤 上傳獎項
                </button>
            </div>
            
            <div class="help-text">
                <strong>檔案格式：</strong>Excel (.xlsx, .xls) 或 CSV (.csv)<br>
                <strong>必要欄位：</strong>gid (獎項代碼), award_list (獎項名稱), participant_count (數量), exec_type (0=一般獎/1=大獎)<br>
                <strong>選填欄位：</strong>reminder_count (剩餘數量，預設同 participant_count)
            </div>
            
            <div class="upload-progress" id="gameProgress">
                <div class="progress-bar">
                    <div class="progress-fill" id="gameProgressFill"></div>
                </div>
                <div class="progress-text" id="gameProgressText">上傳中...</div>
            </div>
            
            <div class="result-message" id="gameResult"></div>
        </form>
        
        <div class="template-download">
            <a href="/api/template/game.xlsx" class="template-link <%= uploadEnabled ? '' : 'disabled-link' %>" download="game_template.xlsx" <%= uploadEnabled ? '' : 'onclick="return false;"' %>>
                📥 下載獎項範本 (Excel)
            </a>
            <a href="/templates/game_template.csv" class="template-link <%= uploadEnabled ? '' : 'disabled-link' %>" download <%= uploadEnabled ? '' : 'onclick="return false;"' %>>
                📥 下載獎項範本 (CSV)
            </a>
        </div>
    </div>

    <!-- 上傳人員資料 -->
    <div class="upload-section">
        <h2 class="section-title">
            <span class="icon">👥</span>
            上傳人員資料
        </h2>
        
        <form class="upload-form" id="personUploadForm" enctype="multipart/form-data">
            <div class="file-input-wrapper">
                <input type="file" name="personFile" id="personFile" class="file-input" 
                       accept=".xlsx,.xls,.csv" required <%= uploadEnabled ? '' : 'disabled' %>>
                <button type="submit" class="upload-btn" id="personUploadBtn" <%= uploadEnabled ? '' : 'disabled' %>>
                    📤 上傳人員
                </button>
            </div>
            
            <div style="margin: 10px 0; padding: 12px 15px; background: #f9f9f9; border-radius: 8px; border: 1px solid #e0e0e0; <%= uploadEnabled ? '' : 'opacity: 0.5;' %>">
                <label style="display: flex; align-items: center; cursor: <%= uploadEnabled ? 'pointer' : 'not-allowed' %>; user-select: none;">
                    <input type="checkbox" id="autoRegister" name="autoRegister" 
                           style="width: 18px; height: 18px; margin-right: 10px; cursor: <%= uploadEnabled ? 'pointer' : 'not-allowed' %>;" <%= uploadEnabled ? '' : 'disabled' %>>
                    <span style="font-size: 15px; color: #333;">
                        <strong>📋 自動報到</strong> - 上傳時自動將所有人員標記為已報到
                    </span>
                </label>
            </div>
            
            <div class="help-text">
                <strong>檔案格式：</strong>Excel (.xlsx, .xls) 或 CSV (.csv)<br>
                <strong>必要欄位：</strong>uid (員工編號), name (姓名), table_num (桌號)
            </div>
            
            <div class="upload-progress" id="personProgress">
                <div class="progress-bar">
                    <div class="progress-fill" id="personProgressFill"></div>
                </div>
                <div class="progress-text" id="personProgressText">上傳中...</div>
            </div>
            
            <div class="result-message" id="personResult"></div>
        </form>
        
        <div class="template-download">
            <a href="/api/template/person.xlsx" class="template-link <%= uploadEnabled ? '' : 'disabled-link' %>" download="person_template.xlsx" <%= uploadEnabled ? '' : 'onclick="return false;"' %>>
                📥 下載人員範本 (Excel)
            </a>
            <a href="/templates/person_template.csv" class="template-link <%= uploadEnabled ? '' : 'disabled-link' %>" download <%= uploadEnabled ? '' : 'onclick="return false;"' %>>
                📥 下載人員範本 (CSV)
            </a>
        </div>
    </div>

    <!-- 非普獎中獎者報到時間分布圖表 -->
    <div class="upload-section">
        <h2 class="section-title">
            <span class="icon">📊</span>
            非普獎中獎者報到時間分布
        </h2>
        
        <% if (registrationDistribution && registrationDistribution.length > 0) { %>
        <div class="chart-container">
            <div class="chart-info">
                <span>🏆 非普獎中獎：<strong><%= registrationDistribution.reduce((sum, item) => sum + item.winner_count, 0) %></strong> 人</span>
                <span style="margin-left: 15px;">👥 對應時段報到：<strong><%= registrationDistribution.reduce((sum, item) => sum + item.total_count, 0) %></strong> 人</span>
                <span style="margin-left: 20px; color: #666; font-size: 14px;">（非普獎 = 獎項代碼非 99999 / 99998）</span>
            </div>
            <div class="chart-legend">
                <span class="legend-item"><span class="legend-color winner"></span> 非普獎中獎人數</span>
                <span class="legend-item"><span class="legend-color total"></span> 該時段報到人數</span>
            </div>
            <div class="bar-chart">
                <% 
                var maxWinnerCount = Math.max(...registrationDistribution.map(item => item.winner_count));
                var maxTotalCount = Math.max(...registrationDistribution.map(item => item.total_count));
                var maxCount = Math.max(maxWinnerCount, maxTotalCount);
                registrationDistribution.forEach(function(item) { 
                    var winnerPercentage = maxCount > 0 ? (item.winner_count / maxCount * 100) : 0;
                    var totalPercentage = maxCount > 0 ? (item.total_count / maxCount * 100) : 0;
                %>
                <div class="bar-group">
                    <div class="bar-label"><%= item.time_slot %></div>
                    <div class="bar-content">
                        <div class="bar-row-dual">
                            <div class="bar-wrapper">
                                <div class="bar winner-bar" style="width: <%= winnerPercentage %>%;"></div>
                            </div>
                            <div class="bar-value winner-value"><%= item.winner_count %></div>
                        </div>
                        <div class="bar-row-dual">
                            <div class="bar-wrapper total-wrapper">
                                <div class="bar total-bar" style="width: <%= totalPercentage %>%;"></div>
                            </div>
                            <div class="bar-value total-value"><%= item.total_count %></div>
                        </div>
                    </div>
                    <div class="bar-ratio"><%= item.ratio %>%</div>
                </div>
                <% }); %>
            </div>
        </div>
        <% } else { %>
        <div style="text-align: center; padding: 40px; color: #666;">
            <span style="font-size: 48px;">📭</span>
            <p style="margin-top: 15px;">目前沒有非普獎中獎者報到資料</p>
        </div>
        <% } %>
    </div>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script>
$(document).ready(function() {
    // Toggle 上傳功能開關
    $('#toggleUploadBtn').on('click', function() {
        var password = $('#adminPassword').val();
        if (!password) {
            alert('請輸入 admin 密碼');
            return;
        }
        
        $.ajax({
            url: '/api/nook/toggle',
            type: 'POST',
            contentType: 'application/json',
            data: JSON.stringify({ password: password }),
            success: function(response) {
                if (response.success) {
                    var statusSpan = $('#uploadStatus');
                    if (response.enabled) {
                        statusSpan.css({ 'background': '#d4edda', 'color': '#155724' }).text('✅ 已啟用');
                        // 啟用上傳元件
                        $('#gameFile, #personFile').prop('disabled', false);
                        $('#gameUploadBtn, #personUploadBtn').prop('disabled', false);
                        // 啟用自動報到
                        $('#autoRegister').prop('disabled', false);
                        $('#autoRegister').closest('div').css('opacity', '1');
                        $('#autoRegister').closest('label').css('cursor', 'pointer');
                        $('#autoRegister').css('cursor', 'pointer');
                        // 啟用下載範本連結
                        $('.template-link').removeClass('disabled-link').off('click');
                    } else {
                        statusSpan.css({ 'background': '#f8d7da', 'color': '#721c24' }).text('🚫 已停用');
                        // 停用上傳元件
                        $('#gameFile, #personFile').prop('disabled', true);
                        $('#gameUploadBtn, #personUploadBtn').prop('disabled', true);
                        // 停用自動報到
                        $('#autoRegister').prop('disabled', true);
                        $('#autoRegister').closest('div').css('opacity', '0.5');
                        $('#autoRegister').closest('label').css('cursor', 'not-allowed');
                        $('#autoRegister').css('cursor', 'not-allowed');
                        // 停用下載範本連結
                        $('.template-link').addClass('disabled-link').on('click', function(e) { e.preventDefault(); });
                    }
                    $('#toggleResult').html('<span style="color: #155724;">✅ 已成功切換</span>');
                    $('#adminPassword').val('');
                } else {
                    $('#toggleResult').html('<span style="color: #721c24;">❌ ' + response.message + '</span>');
                }
            },
            error: function(xhr) {
                var message = '操作失敗';
                if (xhr.responseJSON && xhr.responseJSON.message) {
                    message = xhr.responseJSON.message;
                }
                $('#toggleResult').html('<span style="color: #721c24;">❌ ' + message + '</span>');
            }
        });
    });
    
    // 上傳獎項
    $('#gameUploadForm').on('submit', function(e) {
        e.preventDefault();
        
        var fileInput = $('#gameFile')[0];
        if (!fileInput.files.length) {
            alert('請選擇檔案');
            return;
        }
        
        var formData = new FormData();
        formData.append('file', fileInput.files[0]);
        
        uploadFile('/api/upload/games', formData, 'game');
    });
    
    // 上傳人員
    $('#personUploadForm').on('submit', function(e) {
        e.preventDefault();
        
        var fileInput = $('#personFile')[0];
        if (!fileInput.files.length) {
            alert('請選擇檔案');
            return;
        }
        
        var formData = new FormData();
        formData.append('file', fileInput.files[0]);
        
        // 加入自動報到參數
        var autoRegister = $('#autoRegister').is(':checked') ? '1' : '0';
        formData.append('autoRegister', autoRegister);
        
        uploadFile('/api/upload/persons', formData, 'person');
    });
    
    function uploadFile(url, formData, type) {
        var progressDiv = $('#' + type + 'Progress');
        var progressFill = $('#' + type + 'ProgressFill');
        var progressText = $('#' + type + 'ProgressText');
        var resultDiv = $('#' + type + 'Result');
        var uploadBtn = $('#' + type + 'UploadBtn');
        
        // 顯示進度
        progressDiv.addClass('show');
        resultDiv.removeClass('success error').hide();
        uploadBtn.prop('disabled', true);
        
        $.ajax({
            url: url,
            type: 'POST',
            data: formData,
            processData: false,
            contentType: false,
            xhr: function() {
                var xhr = new window.XMLHttpRequest();
                xhr.upload.addEventListener('progress', function(e) {
                    if (e.lengthComputable) {
                        var percent = Math.round((e.loaded / e.total) * 100);
                        progressFill.css('width', percent + '%');
                        progressText.text('上傳中... ' + percent + '%');
                    }
                });
                return xhr;
            },
            success: function(response) {
                progressDiv.removeClass('show');
                uploadBtn.prop('disabled', false);
                
                if (response.success) {
                    resultDiv.addClass('success').html(
                        '✅ ' + response.message + '<br>' +
                        '共處理 <strong>' + response.count + '</strong> 筆資料'
                    ).show();
                } else {
                    resultDiv.addClass('error').html(
                        '❌ ' + response.message
                    ).show();
                }
            },
            error: function(xhr, status, error) {
                progressDiv.removeClass('show');
                uploadBtn.prop('disabled', false);
                
                var message = '上傳失敗';
                if (xhr.responseJSON && xhr.responseJSON.message) {
                    message = xhr.responseJSON.message;
                }
                
                resultDiv.addClass('error').html('❌ ' + message).show();
            }
        });
    }
});
</script>

</body>
</html>
